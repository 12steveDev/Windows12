# NOTES.txt #

Windows12: El sistema que te vende más humo que los de la esquina xdxd🚬🚬🚬

# === [🛠️] TODO: [🛠️] === #
- FS: Implementar método interno _navigateTo() para ir dentro de rutas
- FS: Implementar metodo list() que devuelva todos los elementos listados en un directorio

# === [💡] IDEAS (solo IDEAS) (o mejor conocido como.. LA COCINA DE 12STEVE🗣️🗣️🗣️🔥🔥🔥🍳🍳🍳🍳): [💡] === #
- OJO PLS PLS: HACER TODO ASYNC PARA PODER TENER DELAYS Y SLEEPS BONITOS
- TextMode y GraphicMode (y cambiar entre uno y otro durante el booteo)
- Lenguaje Bytecode (ECSE)
- - (si el anterior se hace) Permitir código dinámico/no hardcodeado para booteo (ECSE mismo)
- Cuando un programa crea un elemento DOM, asignar automaticamente su attr "data-pid" al pid del proceso
- - Esto ayudará a que cuando se mate un pid, borre automaticamente todo elemento DOM asociado, 0 basura DOM de procs zombis
- Archivos ".exe" ser:
```
- IMAGE_ECSE_HEADER: "12ECSE\x12" (7 bytes)
- IMAGE_CONTENT: JSON.stringify({
    ".header": {
        format: "EXECUTABLE" | "DLL",
        native: true | false, // [oculto, DLLs del sistema]
        version: 1, // más útil que goto🥀
        arch: "ECSE32",//lmao
        subsystem: "CONSOLE" | "WINDOWS", 
        entrypoint: ".text",
        compiledAt: Date.now(),
        DOSStubSecretMessage: "Este programa no puede ejecutarse en calculadora de bolsillo mode.", // alta referencia lol
    },
    ".idata": {
        "USER32.DLL": ["MessageBox", "CreateWindow"]
    },
    ".edata": ["sumar", "miVariable"],
    ".data": {
        "miVariable": "dato inicializado antes de ejecutar!" 
    },
    ".rsrc": {
        "imagenKawai": "images/icons/kawai.png",
        "stringEpico": "(Windows 11 > Windows 12) === !true"
    }
    ".text": [
        ["FUNCTION", "sumar", ["a", "b"], [
            ["RETURN", ["+", ["V","a"], ["V","b"]]]
        ]],
        ["CALL", "MessageBox", "Hi Mundo :3", ["V","miVariable"]],
        ["EXIT"] // "EXIT" termina el proceso, "WAIT" deja el proceso corriendo hasta que se mate solo (tipo programas con ventanas que deben mantenerse ejecutandose)
    ]
});
- IDEOTA: Hacer que todo el contenido (o solamente el código .text) simule ser binario con "btoa()" y al leer simplemente usar "atob()" (NO ES PROTECCIÓN, solamente para recrear eso de que "leo un .exe = bytes ilegibles")
```
- FS dividido entre:
```
"C:": {
    name: "C:",
    volumeLabel: "DriveEpicardo",
    type: "dir",
    children: {
        "CARPETAEPICARDA": { // clave en uppercase para que sea case-insensitive
            name: "CarpetaEpicarda", // guardar name original para case-presserve
            type: "dir",
            creationTime: Date.now(),
            lastModified: Date.now(),
            attrs: 0b0000,
            children: {}
        },
        "ARCHIVOEPICARDO.TXT": {
            name: "ArchivoEpicardo.txt",
            type: "file",
            creationTime: Date.now(),
            lastModified: Date.now(),
            attrs: 0b0000,  // (HIDDEN | READONLY | SYSTEM | ARCHIVE)
            content: ""
        }
    }
};
```
- Un CreateProcess()
- PCB de los procesos:
```
{
    pid: 12,
    path: "C:/Windows/Desktop/miProg.exe",
    cmdLine: "C:/Windows/Desktop/miProg.exe --param1 --param2=jeje",
    cwd: "C:/Windows/Desktop/",   // NUEVO!!
    dllUsed: {}, // { "USER32.DLL": ["MessageBox", "CreateWindow"] } (se actualiza leyendo el .idata del ejecutable antes de crear el PCB)
    argv: ["C:/Windows/Desktop/miProg.exe", "--param1", "--param2=jeje"],
    argc: 3,
    res: exe[".rsrc"], // el programa podrá acceder a sus rsrc
    format: exe[".header"].format,
    arch: exe[".header"].arch,
    exports: exe[".edata"],
    scopes: [{ // scopes para mantener uno global y otros locales (vars modificables globalmente desde una función, etc...)
        vars: exe[".data"] ?? {},
        funcs: {},
    }],
    state: "running" // "running" | "idle" | "terminated"
};
```
- Crear un Registry para poder usar ShellExecute() con extensiones asociadas.
- Guardar el registry en formato XML (para más variedad en el sistema además del JSON)
- Los DLL's importados deberán mantener un estado global cuando sean (1 sola instancia). Tipo "ejecutarse una sola vez" y el resultado de su ejecución (variables y funcs exportadas) quedan iguales para todos quienes las importen
- - Esto permitiria que las DLLs guarden estado interno inaccesible desde afuera. imagina las posibilidades 👀👀

== Ideas para los procesos (tanto "user" como "admin" (que tanto pueden acceder a opcodes especiales)) ==
- `CreateProcess(path, { runAs: "user" | "admin" | "parent", parent: caller_PCB_obj{} })`
|------------------------------|-----------------------|--------------------|
|Proceso Parent (parent.runAs) |CreateProcess( runAs ) |Proceso Hijo Creado |
|------------------------------|-----------------------|--------------------|
| user                         | "user"                | "user"             |
| user                         | "admin"               | Pregunta UAC y luego crea proc "admin" |
| user                         | "parent"              | "user"             |
| admin                        | "user"                | "user"             |
| admin                        | "admin"               | Pregunta UAC (si, siempre) y luego crea proc "admin" |
| admin                        | "parent"              | crea proc "admin" SIN preguntar |
|------------------------------|-----------------------|--------------------|
- Las DLL's no tienen permisos ni nada, solamente usan el runAs del programa que las callea
== Arquitectura de ECSE ==
-> OP Públicos:
- - ADD, CONSOLE_LOG, SET
-> OP Privilegiados (necesitan runAs: "admin"):
- - FS_WRITE_FILE con rutas internas (C:/Windows/, etc...)
-> OP Reservados (necesitan native: true):
- - FS_WRITE_FILE, PM_KILL

- lo "native: true" solamente sirve para que pueda ejecutar los OPCODES reservados directamente, que para un programa normal le tomaría usar funciones exportadas de DLL's nativas
- al cargar las DLL's, ejecutar su código con el PCB de el proceso caller TENIENDO EN CUENTA que si tiene native: true, añadir "native: true" a la copia del pcb del caller que se le pasa a ECSE para que habilite los OPCODEs reservados al ver "pcb.native"

# === [❓] DUDAS: [❓] === #
- El BSOD será un "div encima de todo con carita triste y la info del error" o "cambio a modo texto e imprimo la info del error en fondo azul"!??!??
- explorer.exe corre como user o admin!??!?!?!?!?!? (los procesos creados desde explorer obviamente se sabe que serán "user" o "admin" si se especifica")
- la manera en la que se almacenarán los elementos DOM creados por un pcb/proceso, ¿cómo será?
- - 1. Al crear el elemento DOM, asignarle su "data-pid". Así al matar el proceso simplemente buscar elementos con el data-pid igual
- - 2. Al crear el elemento DOM, pushearlo en un array dentro del PCB del proceso. Así al matar el proceso simplemente hacer "pcb.elems.forEach.remove()"
- - - 1.5: Usar el data-pid y a la vez guardar el elemento DOM en un array dentro del PCB. No entiendo la necesidad de esto la verdad
- - 3. Por cada elemento DOM creado, asignar un ID UNICO a ese elemento y en el PCB SOLAMENTE añadir ESE ID, y al matar, buscar cada elemento por su ID. Esto es claramente innecesario y opcional.... pero esa es la idea no?👀🚬
- ¿permitir que cada "fs" pueda especificar si es case-sensitive o no!?? aunque el lío con esto es que ahora métodos como "makeDir" o "writeFile" tendrán que comprobar si el FS es o no case-sensitive y basado en eso crear la key que se pondrá en el FS........ otra featura completamente innecesaria XDXDD
- Y la duda más importante... ¿Microsoft está temblando?... 👀🍳🔥‼️
# === [📄] NOTAS (irónico): [📄] === #
- QUIERO OBLIGAR a que los programas usen funciones exportadas por .dll's, pero a la vez que las DLLs si puedan usar los OPCODEs que llaman directo a JS. Ejemplo:
- Notepad quiere guardar archivo
- Notepad NO puede usar "FS_WRITE_FILE" porque no tiene "native: true", por lo que el OPCODE ni existe para el
- Notepad recapacita y hace inteligentemente "CALL WriteFile", a la función WriteFile que le proporcionó kernel32.dll
- ECSE ejecuta el código de la función WriteFile (la cual usa FS_WRITE_FILE)
- ECSE ve que kernel32.dll es native, por lo que ejecuta el OPCODE
- Notepad guardó tu archivo .txt todo qlero con éxito tras varias abstracciones totalmente inútiles pero necesarias, bravo!!

Flujo (esperado) de Windows12:
- Se ejecuta booteo (sea hardcodeado o dinamico por drive)
- Boot cambia modo texto a modo gráfico (si se llega a implementar)
- Boot ejecuta "C:/Windows/explorer.exe"
- explorer lista todo en "C:/Windows/Desktop/" y los muestra obteniendo los íconos asociados con las extensiones
- usuario hace doble click en ícono:
- - *Probablemente* se ejecuta "ShellExecute()" que resuelve asociaciones (.png, .txt, .html, etc) y termina llamando al CreateProcess() con los programas y args necesarios
- - PCB vive hasta que termina el programa

# === === === === === === === #
#     [⏳] CHANGELOG [⏳]     #
# === === === === === === === #
=== 05/06/2026/Fri ===
- Mostrar iconos hardcodeados para dar por INICIADO el proyecto XDXD (index.html)
=== 15/06/2026/Mon ===
- Implementar ERROR_*'s para manejo de errores (utils.js)
=== 16/06/2026/Tue ===
- Implementar `FS.splitPath(path, pcb)` (fs.js). Permite:
--> Detección de unidades ("C:", "Z:"), ignoración de "/" repetidos absurdos, "\" a "/"
--> Rutas absolutas con unidades ("C:/Windows/Desktop")
--> Rutas absolutas a la unidad actual (pcb.cwd) ("/Windows/Desktop")
--> Rutas relativas al cwd ("./images/something.png", "imates/something.png")
--> Resolución de "." y ".." intermediarios ((cwd="C:/Windows/Desktop/"), "./../System/" -> "C:/Windows/System/", "../" -> "C:/Windows/", "./../../../../../../" -> "C:/")
